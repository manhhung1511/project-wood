import React, { useState } from "react";
import styles from "./BuyStep.module.scss";
import classNames from "classnames/bind";
import Footer from "../Footer";

const cx = classNames.bind(styles);

const BuyStep = () => {
    return (

    );
}   

export default BuyStep;